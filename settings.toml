# ============================================================================
# User Configuration Overrides
# ============================================================================
# This file overrides defaults from cli/config/defaults.toml

# ============================================================================
# User CLI Settings
# ============================================================================

[cli_settings]
installation_directory = "$HOME/.tmp/inumaki-dotfiles"
backup_directory = "$HOME/.tmp/backup"
install_type = "update"
hidden = true
rebuild_containers = true

[cli_settings.debug]
log_level = "info"
output_to_file = true
log_directory = "/tmp/dotfiles/logs"

# ============================================================================
# Project Configuration Overrides
# ============================================================================
# NOTE: Path overrides should be done in directories.toml or using dynaconf
# merge syntax to avoid replacing entire sections
